1) Не менять название дирректорий после клонирования гит-репозитория.
   Обязательно запускать команды из директории с докерфайлами. 

2) Соберет образы и запустит контейнеры:
   docker-compose up

3) Если надо добавить еще один вычислитель и подключить к сети:
   docker run -d --name=calculator_optional1 --net meriterra_back meriterra_calculator1
   
   Можно сколько угодно добавлять, главное, чтобы --name не повторялись

4) Логи о количестве запросов-ответов и их продолжительности появятся в директориях client1_dir и client2_dir,
   которые будут созданы автоматически при запуске.

5) Для выключения всех сервисов:
   docker-compose down
